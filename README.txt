E30Heaven Configurator V4

This version is self-contained and needs no external libraries.
The vehicle preview is rendered dynamically as SVG in the browser, so body type, model, colour, wheel design and camera angle all change the image immediately.

Upload the contents of this folder to the root of the GitHub Pages repository and replace the previous index.html.
